android {
            defaultConfig {
                applicationId = "org.nativescript.appaticaconductor"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }